import React from "react";

const ProviderDashboard: React.FC = () => {
  return (
    <div className="p-6">
      <h1 className="text-xl mb-4">Healthcare Provider Dashboard</h1>
      {/* TODO: Add patient lookups, incoming requests, records access */}
    </div>
  );
};

export default ProviderDashboard;
